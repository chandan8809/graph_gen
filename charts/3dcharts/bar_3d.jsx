import React from 'react';

// Extremely simple component with no dependencies
const BarChart = () => {
  return (
    <div>
      <h3>Simple Bar Chart Placeholder</h3>
      <p>This is a simple placeholder component to test imports.</p>
    </div>
  );
};

// Make sure the export is explicit
export default BarChart; 